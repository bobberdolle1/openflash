[package]
name = "openflash-core"
version = "1.9.0"
edition = "2021"
description = "Core library for OpenFlash NAND/eMMC programmer"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
proptest = "1.4"
